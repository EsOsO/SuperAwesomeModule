name: Tests
on: [push]
jobs:
  pwsh_tests:
    name: Module tests on ${{ matrix.os }} - ${{ matrix.shell }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macOS-latest]
        shell: [pwsh, powershell]
        exclude:
          - os: ubuntu-latest
            shell: powershell
          - os: macOS-latest
            shell: powershell

    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Run Pester tests
        shell: ${{ matrix.shell }}
        run: ./build.ps1 -Task Test
